function mostrar() {
	let num;
	let div = 0;

	do {
		num = prompt("Ingrese número");
	}
	while (num <= 1 || isNaN(num));
	for (let i = 2; i < num; i++) {

	} if (num % i == 0 && i > div); 
	{
		div++;
	}
}
if (div >= 1) {
	alert(num + "No es primo");
}
else {
	alert(num + "Es primo");
}

//FIN DE LA FUNCIÓN