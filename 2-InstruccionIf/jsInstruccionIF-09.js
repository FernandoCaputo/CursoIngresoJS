/*function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	let	Maximo = 10;
	let Minimo = 1;
	let Num;

	Num =	Math.round(Math.random() * (Maximo - Minimo) + Minimo);
	
	alert(Num);
	
	

}//FIN DE LA FUNCIÓN*/

function mostrar(){

let maximo = 10;
let minimo = 1;
let num;

num = Math.round(Math.random() * (maximo - minimo) + minimo);

alert (num);
}
